// Hand-picked Gallery keyframe times (#888), in Pattern milliseconds. A name
// listed here pins its poster time instead of the scored choice. Re-run
// `npm run gallery:keyframes` after editing.
export const GALLERY_KEYFRAME_OVERRIDES: Record<string, number> = {}
