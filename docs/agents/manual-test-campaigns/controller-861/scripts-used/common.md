# Manual Controller testing — execution contract

Drafts validated against physical UI before check-in. Covers hardware batches only; automation is paused. Use Astra Low and Computer Use in one prepared dedicated Chrome for Testing instance, existing local-agent-32 profile at localhost5176. Root launches existing setup-only with tty:true before Computer Use inspection (inspection can otherwise launch an empty browser). No Chromium account sign-in. No automated scenario runs or code repairs.

Fixed actions/outcomes below; navigation may adapt to current visible UI. Record every deviation with expected/observed/action/validity. On capture error inspect fresh state: continue if effect is unambiguous, never duplicate an uncertain mutation. AX or screenshot can establish state; both are not universally required. Stop an unusable step after5min, on permission denial, unsafe uncertainty, or Controller connection refusal. For refusal ask Jon to restart Controller first. Independent valid cases may continue after a blocked case. Do not relabel failure as pass or silently relax an outcome.

One Controller owner: explicit Studio disconnect before native tab, close native tab before Studio reconnect. No additional WebSockets or hidden state/protocol writes. Read-only API/DOM metadata and binary map download may supplement visible proof; label them. No unsupported close-event requirement after page navigation.

Before each batch capture actual exact stable identity, active saved ID,16 baseline Controller IDs/names, brightness0.01,256 pixels, valid2D map bytes/hash, sequencing Off/not running, renderer running, profile Limit power off/fixed cap25%, Keep up to date off, no hardware inputs, and no managed baseline IDs in this synthetic profile. Expected bench is Burner bag192.168.8.224, pixelblaze_pb32_3cd4ee549434,pb32/3.67. Do not normalize a different baseline silently. Prior authoritative baseline: /private/tmp/pxlblz-controller-acceptance-861-1788885545804/baseline.json; compare with actual observations. No need to repeat already valid checks excessively within one connected batch.

Use unique batch tag; record each created Studio/Controller ID immediately. Never select deletion by prefix/name alone; verify exactID via UI metadata/dialog. Never mutate a foreign Pattern's source or saved control defaults. Preserve prior Trash. Use library Move to Trash for owned Studio records; editor Delete for an already trashed record may be permanent and is NOT authorized.

Cleanup every batch: restore exact baseline active ID, brightness, count, map bytes, renderer, sequencer and profile settings; delete only owned Controller IDs after switching away; trash only owned live Studio IDs. Verify fullbaseline inventory and owned absence, exact map hash, config plus visible facts. Explicitly disconnect. Report discrepancies; never claim cleanup from hidden rail rows alone.

PASS requires expected visible outcome plus specified preservation evidence. Other verdicts: PRODUCT-FAILURE only for demonstrated contradiction; EXECUTION-ERROR; BLOCKED; NOT-TESTED. SW7/RC5 are agreed exclusions, notpasses. Screenshots+concise action/observation log percase; exactledger+baseline/after-state and elapsedtime. Aim25min perbatch, reserve cleanup; report before extending. Coordinator handles any approved continuation. No batch/wholeissue completion claim until all required outcomes and cleanup verified.

## Accepted baseline adjustment
Jon explicitly accepted the LP2 precision-restoration anomaly as minor and directed continuing major testing without fixing it. From this point use persisted brightness0.01 (1%) as the restoration baseline. The historical baseline JSON contains0.012769; that one field is intentionally superseded. Keep LP2 recorded as an accepted execution anomaly, not an unqualified PASS. All other restoration requirements remain. Do not spend more time on precision restoration or change brightness in later cases unless explicitly required.
