# Batch5 — managed reconciliation
Read common.md. Precondition no baselineControllerID managed by this syntheticprofile; nohardwareinputs; Limitpoweroff, fixedcap25%, Keepuptodateoff. If not met, stop—never change ownership to forcecoverage.

Create/save3 uniquelytagged run-owned Studio Patterns A/B/C with smallvalidsource `export function render(index) { hsv(0,0,0.02) }`. Record all6 exactIDs. C active. Record completeforeignbaseline inventory.
RC1: Verify each managedrowCurrent, C running. Enable Keep PXLBLZ Patterns up to date when Controller settings change.
RC2: Turn Limitpoweron. Observe realreconciliation and eventualCurrent on all3 rows. PASS must distinguish actualrewrite from oldCurrentlabels: capture queued/updating→Current when visible, or readback produced Controller artifacts showing changed emittedfixed-cap content for each ownedID. A bounded supporting oracle is to download the three owned /p/<exactID> files through read-only HTTP before the toggle and after Current, then compare their hashes: every owned artifact must actually change. Keep IDs and paths explicit. Capture the UI Current/active result as well. Read-only metadata/exports may support this; no new instrumentation or provider writes. If transition too fast and no suitable readback, mark evidence incomplete rather than assume. Retain active-last evidence only when observable; not a fleetingtext-only oracle.
RC3: ForeignIDs/names unchanged; C remainsactive. Do not claim bytecodeequality from ID/name equality.
RC4: Turn Limitpoweroff; repeat actualrewrite+eventualCurrent evidence for all3; same IDs, C active, foreign inventory unchanged. If using saved-file readback, every off artifact must differ from its on artifact; do not merely reread stale Current labels.
RC5: NOT-TESTED agreed exclusion: no inducedrewritefailure. Unexpectedfailure remainsfailure, never an exclusion.

DisableKeepuptodate; ensureLimitpoweroff/capunchanged. Restorebaselineactive, delete3 exactownedControllerIDs, trash3 exactownedStudioIDs. Verifyfullbaseline/settings/map. Report everyevidencelimit. Do not expand into timing/racefaults or runanotherbatch.
