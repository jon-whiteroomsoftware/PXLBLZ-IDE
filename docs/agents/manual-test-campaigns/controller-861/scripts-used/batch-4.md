# Batch4 — maps and pixel count
Read common.md. Before mutation establish a supported UI restoration path for the exact baseline map. The existing matching baseline Studio map may be in Trash: do not restore/rename/delete it; its direct editor route can be inspected and used for the authorized map send. If no exact restoration path exists, stop before replacing the map. Save baseline rawpixelmap bytes/hash as supporting evidence.

Create owned fixed256-point2D map with coordinates [column/15,row/15] over16x16. Use a literal256-entry JSON array (generate fixture text offline if convenient), not a pixelCount-dependent function. Record newStudioID and exactsource. Also prepare fixed64-point8x8 array for MP4. Names unique.
MP1: Send256 map through confirmation. Verify fresh installed identity in profile+panel,2D,256points,Controller256 and no mismatch. Readback bytes supplement visibleproof.
MP2: Unchanged secondsend is gated with no-changes explanation; verify focus/hover explanation.
MP3: Profile Import map offers Open matching Studio map with sameidentity,256px/2D/16x16. Open it; verify exactexistingStudioID, not duplicateimport.
MP4: Open64 map and Send. Verify visible64points/256pixels mismatch and offered map/count choices. Cancel. Refresh; verify count256 and exactsame installed256mapbytes as beforeCancel. Never install invalidmap.
PC1: Apply Controller count200 through inlineeditor. Verify live256-map-versus200-pixels mismatch.
PC2: Restore256 through UI. Verify mismatch disappears, installedmapidentity unchanged. Resume renderer if needed to clear physicaltail as documented; record any such adaptation.

Restore exactbaseline map via establishedUI path and verify hash equality, count256, baselineactive/settings andinventory. Trashonly2 ownedmaps, preserve existingbaselineTrash. No permanentdelete, no otherbatch. Report cases+cleanup.
