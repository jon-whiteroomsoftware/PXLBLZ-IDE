# Selected-time appearance integration evidence

Jon approved the nominal `[start,end)` selected-time boundary on2026-09-16.
Outside-bar contribution requests refuse atomically; legal first/last keys still
own inherited contribution. This slice does not claim whole#1038 completion.

## Opposite Effect order: unresolved representation

`opposite-order.json` contains one Clip from0 through1000 with one runtime and
no animation, Groups or Layout changes. Hue precedes Invert initially. An
interior selected-time reorder at200 makes Invert precede Hue thereafter.
The preimage prepares successfully. Both the selected candidate and independently
authored adjacent Clips sharing the same runtime refuse preparation with
`unsupported-runtime-sharing`.

`showModel.ts`'s `showRecordToCompileRecipe` detects conflicting shared Effect
order and allocates `showEffectOrderVariantClipId` (`instance~fx1`). The native
preparation cast check in `showCompositionLoweringV2.ts` refuses that extra member:
there must be exactly one executing runtime per used Pattern instance. Raw legacy
compilation accepting two members does not prove shared private state. The pure
appearance owner preserves the correct authored graph. This is unresolved recipe
representation work, not an accepted product refusal/domain limit or supported
reorder output coverage. No recipe/compiler changes occur in this slice.

## Precise opacity: exact measured section-origin discrepancy

`opacity-precision.json` contains the exact preimage and selected key candidate,
prepared recipes, emitted and Fixed sources, imported EPE delivered frames and
exported state at250 in both modes. The original opacity sine/in-out track is
unchanged: activation[100,300), base.2, delta.3, duration200.

The preimage's opacity expression uses `((showTime-.1)*1000)/200`. The selected
second section uses `(100+((showTime-.2)*1000))/200` with the same sine kernel.
The emitted subtraction origin and addition association differ; persisted track
values, identities and curve are unchanged. In Precise, the first red channel
changes from.147705078125 to.1477203369140625, exactly1/65536. The other five
channels and all exported private/control state are identical. Fast frames and
state are identical. This measures a section-origin difference in fixed-point
playback; it does not establish byte-exact unsplit preservation.

The separate independently authored complete-key expected candidate produces
exactly equal Fast/Precise frames and exported state. That equality oracle stays
exact; no tolerance is widened. The measured unsplit comparison is pinned as
explicit diagnostic evidence. No lowerer/compiler repair is included.

## Existing presentation and participant restrictions

Freeze/Strobe with keyed instance controls produces a structurally valid selected
candidate and ready preparation, but compilation requires a static unkeyed cache
span. The admitted unkeyed cache consumer has actual native EPE Fast/Precise
proof separately. This classification is not keyed cache output-preservation proof.

Divergent held appearance on an animated positive participant Transition endpoint
can retain named `unsupported-transition-property-track` refusal in final
preparation. Numeric Effect participant ramps remain structurally preserved under
their existing unsupported final ramp family. Full preparation/compilation belongs
to final caller adoption, where refusal leaves the original document unadopted.
The pure owner does not compile source, delete animation, project ramps or widen
these domains.
