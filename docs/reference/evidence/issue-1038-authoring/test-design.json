{
  "invariants": [
    "Persisted Group occurrences always carry an authored holds array whose IDs are nonblank and unique, positions are safe strictly increasing interior definition times, durations are positive safe integers, and checked occurrence duration never overflows.",
    "Definition duration stays unchanged; occurrence duration is definition duration plus each hold exactly once and protects collision, Show End and every intersected Layout interval.",
    "Child and activation boundaries use the accepted half-open before/after mapping; a strict spanning child retains one identity and includes held time.",
    "Held appearance and Property values equal the source right-boundary value, nonlinear and discontinuous curves resume from the original source domain, and authored key identities remain attached to authored times.",
    "Materialization preserves definition bytes and effective runtime identity; a hold creates no runtime and no Restart event.",
    "Internal Transition duration and identity stay unchanged; strict interior and endpoint holds refuse when exact participant attachment fails, while a hold before the complete window shifts it intact.",
    "The public Property Insert Time API and Group projection use one acyclic exact time-mapping kernel with unchanged public signatures."
  ],
  "partitions": [
    "Missing or empty holds; one and multiple holds; blank/duplicate IDs; ordered, equal and unordered positions; positions at zero, strict interior and definition end; zero, positive, noninteger, unsafe and overflowing duration.",
    "Child ending at, starting at, strictly spanning, before and after a hold; one hold and ordered multiple holds.",
    "Appearance key absent or exact at a hold; Property key inside a nonlinear segment, exact discontinuity and last active key.",
    "Internal Transition hold before the window, at start, strict interior and at end.",
    "Ordinary and Group instance-target owners with overlap and exact half-open adjacency.",
    "One continuously available Layout, retained Zone across a switch, unavailable Zone after a switch, collision and Show End overrun.",
    "Continue and Restart entry policies; explicit and default runtime bindings; Fast and Fidelity replay.",
    "Ordinary short fixtures and a 60000 ms persisted Show without widening the millisecond domain."
  ],
  "sequences": [
    "Reopen valid held bytes, remove holds, then mutate every identity/order/range/duration partition and prove immutable refusal.",
    "Materialize two ordered holds through appearance and a quadratic Property track, reopen, and evaluate before, within and after both hold intervals.",
    "Map children ending and starting at one hold, compile the held Group and an independently authored ordinary-v2 record, reopen the generated artifact, and replay boundary probes in both runtimes.",
    "Move a hold from before an internal Transition to its start, interior and end; preserve the intact shift and refuse every detached participant window.",
    "Extend an occurrence across a Layout switch, retain then remove its Zone, and overrun Show End.",
    "Bind a Group instance track to an ordinary runtime with an active owner and refuse the materialized overlap without changing the candidate.",
    "Derive Restart and effective-use information after holds and assert one mapped entry event on the same runtime."
  ],
  "oracles": [
    "Provisional structural plus domain codec reopen, complete validation and exact input equality.",
    "Complete materialized ordinary entities with independently calculated timestamps, durations, IDs, curve descriptors and evaluator values.",
    "Public validateShowLayoutAvailabilityV2 output names the held Group occurrence and every unavailable Layout interval.",
    "Public prepareShowV2ForCompile output compiles; generated .epe reopens; exact quantized frames and state within 1e-12 floating association agree with the independent ordinary-v2 record in Fast and Fidelity.",
    "Public deriveShowRestartEventsV2 and effectiveShowInstanceUseCountV2 prove one entry event and unchanged runtime use.",
    "Existing Property animation regressions prove the extracted kernel preserves the landed public Insert Time behavior."
  ],
  "faults": [
    "Swap the strict and inclusive hold predicates in before/after mapping.",
    "Omit or double-count a hold in occurrence duration.",
    "Stretch an easing curve through held time or discard its retained curveSegment offset.",
    "Move an authored key ID to a synthetic boundary or reuse a colliding derived ID.",
    "Create a Restart event during the hold or mint a new runtime during materialization.",
    "Validate only the occurrence start Layout and miss its hold-extended interval.",
    "Check conflicts before Group materialization and admit an overlapping effective instance target.",
    "Retain a Transition whose mapped participant endpoints no longer match its unchanged duration."
  ],
  "mutationEvidence": [
    "The first extraction-aware qualification exposed two no-coverage arithmetic mutants in retained curveSegment offset mapping; a descriptor whose authored left time is nonzero made both signs observable.",
    "The repaired targeted Show-authoring qualification killed all 166 selected mutants with zero surviving, timeout, uncovered or errored mutants."
  ],
  "residualGaps": [
    "Global Insert Time, repeated insertion into an existing hold and Marker ripple remain the later #1038 timeline owner.",
    "Group duplicate, Make Unique and ungroup lifecycle operations remain the later #1038 Group edit owner.",
    "The disjoint #1038 Layer owner supplies Layer authoring and integrated held-Group Layer proof after this schema lands.",
    "The coordinator's scheduler corrective supplies the remaining integrated Restart proof; this slice does not edit showCompiler.ts.",
    "Pilot history/save/provider adoption and UI remain later #1038 integration slices.",
    "The coordinator owns final committed-tip suites, exact-range review, landing, labels and publication."
  ]
}
