# Ordinary Clip sharing controls

Assignment: gpt-5.6-sol/high, 2026-09-16. Stack base Property0fd353c75797f2ef3652a541c59aa3c5086aebb5; canonical specification51a5c2acf2e1732610e1ee4c2e3d4e29f934511e §§4/6/9/12. Assigned proof SHARING/RESTART/ACTIVATION/ROUTE/FAILURE/PARITY. Coordinator owns technical issue state and final delivery gates.

The pure model and thin panel operate only on the explicitly selected ordinary Clip. Linked Duplicate requires an explicit Zone, matching Layer and integer start. Make Pattern Independent copies the exact effective instance-track/key plan; Rejoin requires an explicit compatible authored top-level runtime. Effective Group users count for sharing and retention. Unhoisted Group-only default runtimes cannot be Rejoin targets under the current owner contract and are not offered; this corrects the broader dispatch phrase without extending owner semantics.

```
Pattern sharing
Instance <persisted identity> · <effective Clip use count>
Duplicate Zone [Choose]   Layer [Choose]   Start [ms]
[Duplicate linked Clip]
[Make Pattern Independent]
Rejoin instance [Choose compatible persisted identity]
[Rejoin Pattern]
```

The panel follows existing zinc fields, native labelled inputs, small outline buttons and unboxed spacing. Destination fields start empty. Sharing text explains that linked Clips use the same controls, clock and private state; Rejoin text explains that the chosen instance supplies its current state and animation. These consequences prevent confusing Clip duplication with runtime independence or an animation merge. No decorative captions or alternate scope are introduced.

Seams: public model projection and exact intent allocation; panel typed submission; existing editShowClipV2; shared prepared adoption through the future closed sharing branch. Group UI exclusively owns its additive admission/Route integration until its immutable checkpoint. No new runtime/compiler/schema rules, Replace, deletion, Group editing or independent-duplicate composition are in this slice.

Oracles: exact complete identity maps and allocation counts; immutable qualified capture, linked held Group uses and global/local track preservation; malformed/stale/refused/no-op zero history/save; one successful adoption/write and owned rollback; imported native EPE Fast/Precise frames plus private state against independent records. Final actual Worker/browser 30-second sequence covers duplicate, independence, explicit Rejoin, Undo/Redo, provider reload and native artifact reopen with visible Stage, desktop/narrow keyboard and zero errors.
