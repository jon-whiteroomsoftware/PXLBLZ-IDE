# Diagnostic waiting proof

Source: `8b33715a2ba83c9dd8b54017e666db90b19a0bb6`.

The real scripted bridge and editor ran on the managed shared runtime at
`localhost:5178`, with synthetic identity `local-agent-04`. Explicit internal
`dirty-field` tokens produced the waiting state. They do not establish real
manual field or gesture registration.

The [route record](route.json) records four cases: release saved exactly one
8000ms resize; Cancel, the original five-second timeout, and Library metadata
invalidation each preserved complete visible and durable records with zero
writes. The accepted `.pxlshow` was downloaded through Show actions and reopened
through the production importer; its complete Show equalled the current record.
No page errors occurred. Visible and durable refusal baselines are compared
separately because the fixture's loaded record adds `restartOnEntry: false`.

Desktop1440 and narrow800 captures are committed under
`.wrsp/ui-proof/949-waiting-{desktop,narrow}.png`, with exact source records beside
them. The existing reference-map delivery diagnostic remains visible; no
Controller delivery readiness is claimed. The existing diagnostic panel placement
is unchanged, and is not the final #959 panel proposal.

The in-app browser connected, but its supported read-only evaluation and
capabilities could not establish an isolated synthetic cookie or activity token.
The disclosed fallback used repository Playwright with a separate synthetic
context. After omission probes, Vite's loaded module URL carried an HMR identity;
using that exact observed URL made the synthetic token belong to the live store.
A bare module import had created a distinct store and issued no token. Final
captures were taken with stable committed source, after fault restoration.

Verification:143 focused protocol tests;169 staged tests in normal commit hooks;
TypeScript and lint; four [behavioral omission faults](omissions.json). The
repeatable W case in `e2e/agent-baseline.auth.spec.ts` passed through the repository
isolated authenticated runner (8.9s test,14s wrapper). The additional four-case
capture runner and full logs remain under ignored `.wrsp/949-diagnostic-wait/`.
The [test-design packet](test-design.json) preserves the remaining qualification
limits. Final committed-tip suites and native review belong to the coordinator.

CONTEXT.md was checked: this slice changes diagnostic protocol consumption and
introduces no new domain term. Candidate contract, Technical Reference, baseline,
and harness provenance were updated; the current-user Feature Guide remains
unchanged because real manual activity registration is still outstanding.
