# Typed exact resize evidence

The finite diagnostic takes an explicit logical Clip id and positive safe-integer
duration before inference. Its existing store capability observes dependencies
throughout the request and replays a matching proposal on the current Show.
Ordinary chat remains broad. This packet does not qualify unrestricted text,
model-selected timing, generic grammar batches, paid inference or hosted service.

The browser sequence `TR` in `e2e/agent-baseline.auth.spec.ts` exercises the actual
DEV API, delayed scripted `/resize` HTTP route, manual controls, authoring
validation and provider persistence. Four cases cover independent Layer duration
commit, same-Layer conflict, dirty-field Escape and dirty-field Enter. Accepted
work is compared as complete visible/durable records and downloaded `.pxlshow`
reopened through the importer; Undo preserves earlier independent manual work.
The finite transport packet and complete PATCH body are retained in route proof.

The provider test uses the installed OpenAI SDK with injected fetch and a
synthetic accounting ledger. Its complete request snapshot was inspected: only
two fixed messages, target/duration, two tool schemas and existing budget fields
are present. Invariant assertions independently check message roles, exact user
packet, schema names/keys/constant bindings and fixed tool outputs. Broad reads,
foreign arguments, missing/contradictory completion, abnormal return and extra
request context are rejected without a proposal.

Four controlled faults were detected and restored: dependency recapture admitted
same-Layer/shared/ABA changes; inherited history changed the actual provider
request; foreign-target acceptance violated terminal rejection; stale full-record
replay lost independent manual work. [Fault results](faults.json) and
[test design](test-design.json) identify the boundaries. Initial tests failed on
missing protocol/adapter/turn APIs and ephemeral invalid-proposal rejection.

The initial browser selector addressed two identically named Clips by index and
edited Main. Complete record comparison established that same-Layer refusal was
correct. Distinct accessible Pattern names resolved selection. Case teardown now
awaits the actual delayed response because conflict observation can refuse before
HTTP completion; the bridge busy guard remains unchanged.

Final committed-source browser captures and complete route records are attached
separately. Authoritative final suites and native review are coordinator-owned.
