# Manual resize convergence evidence

Pointer and composition-inspector duration commits now share the exact resize
owner. This is semantic-owner convergence, not a full #950 completion claim.
The store's narrow Layer admission, broader private batches and live active-input
waiting remain separate qualifications.

The finite engine matrix compares whole Shows for Main and overlay fixture R at
7999/8000, 8001/12000 refusal, no-op4000, invalid inputs and missing target. It
preserves a shared instance track, checks source immutability and reopens exported
`.pxlshow` files. A valid multi-Scene connected fixture checks complete parity,
9000–21000 target extent, successor at22000 and retargeted Transition identity.
Connected growth/shrink/leading-edge cases and the explicit incoming-zero manual
Cut exception are separate from agent unsupported-topology refusal.

The historical inspector fixture had a Layer Transition crossing Scene-local
Layers. The real file importer rejects its composition; the repaired Show imports
through the same envelope. The lower-level historical repair assertions remain,
and a distinct initially valid fixture proves supported retarget behavior.

Component pointer cases check bounded feedback without writes, one commit/save,
undo/redo, cancellation, no-op, queued save failure recovery, attached Detail
restoration, stale metadata/timing refusal and the existing Scene-boundary Cut.
Group isolation remains on its original owner. The real paired route is in
`e2e/agent-baseline.auth.spec.ts` case R: pointer12000→8000, inspector7999,
scripted canonical8000, no-op and refused12000, downloaded Show reopen and history.
The scripted bridge makes no paid inference or device call.

In-app browser discovery returned only Chrome; two `iab` probes returned
`Browser is not available: iab`. Repository Chromium is the disclosed fallback.
Focused verification passed 56 cases after all omission sources were restored,
including the Group isolation case; full-project TypeScript passed. The real paired
route passed in26.7s (50.7s wrapper). [Route records](manual-route.json) retain
complete results. All [three targeted omission faults](manual-omissions.json)
failed their intended behavioral assertions: dispatch output, stale overwrite and
duplicate undo history. [Review test design](manual-test-design.json) records the
five-array qualification. Final full suites and native review remain coordinator-owned.

Documentation sweep: the command, state/history and candidate contracts, Technical
Reference and Feature Guide reflect this finite scope. CONTEXT.md was checked;
no domain term changed. The forward-looking census and remaining issue scope are
unchanged. The public README was not edited.
