{
  "issue": 1038,
  "scope": "section-scoped Property-track activation beside participant Transitions, plus registered Group creation and Group occurrence route specs",
  "specification": {
    "path": "docs/plans/scene-retirement-specification.md",
    "commit": "51a5c2acf2e1732610e1ee4c2e3d4e29f934511e",
    "proofRows": [
      "INSERT",
      "GROUP-HOLD",
      "ACTIVATION",
      "PARITY",
      "ROUTE"
    ]
  },
  "base": "3fb7e307fd24e491a694a046ce1d0a0004cc572e",
  "invariants": [
    "A track has effect only in [activeStartMs, activeStartMs + activeDurationMs); the presence of a participant Transition never widens or narrows that interval.",
    "Per intersected section the activation is clipped to the section, keys and curveSegment descriptors are retained exactly under half-open evaluation, and nothing is emitted for a section the activation does not intersect.",
    "A shared instance track maps once per section, never once per consuming Clip.",
    "No normalization by endpoint-value difference: a restricted interval keeps the source kernel, not a re-normalized two-point curve.",
    "A record whose Clip and instance tracks all cover [0, showEndMs) keeps the existing single-section emission and its generated bytes.",
    "Transient section track and keyframe identities are collision-safe and never rewrite persisted identities.",
    "A participant Transition rides inside the one section that owns its whole window; a boundary at or inside that window refuses atomically with the preimage intact.",
    "No emitter, schema, runtime or compiler-domain widening; RL08-RL10 and the multi-key-Clip guard are unchanged."
  ],
  "partitions": [
    "activation exactly [0, showEndMs) versus section-scoped, with and without a participant Transition",
    "instance-control target versus Clip-view target versus shared instance target consumed by two Clips in different sections",
    "activation clipped across a section boundary another track introduces, at an authored key and mid-segment",
    "retained curveSegment kernel versus re-normalized two-point restatement",
    "activation edge strictly inside a Transition window; exactly at the outgoing end; exactly at the incoming start",
    "materialized Group instance-control activation beside an ordinary positive Transition and beside an internal Group-local Transition",
    "global Insert Time at p = 0 shifting a full-Show activation off zero"
  ],
  "sequences": [
    "author partial activation -> prepare -> compile -> export .epe -> reopen -> replay Fast and Precise against an independently authored legacy record",
    "Insert Time interior refusals -> gap insertion -> exact discontinuity key -> last active key -> Layout switch -> p = 0, each through real admission with history and provider save",
    "Group hold example -> repeat insertion -> move -> duplicate -> Make Unique -> Ungroup, then the overlapping instance-track duplicate refusal on a non-colliding Layer",
    "Group definition-local Replace with the definition's instance animation retained beside an ordinary participant Transition"
  ],
  "oracles": [
    "Independently authored legacy v1 records whose Scene-scoped tracks express the same activation, compiled through showRecordToCompileRecipe: exact frames and exact exported gain/tilt/elapsed for all three members at 22 probe times in Fast and Precise. Endpoints are binary-exact so the comparison needs no tolerance.",
    "Reopened .epe through parseEpe for every candidate probe; the candidate is never judged against another v2 candidate.",
    "Retained-kernel case: delivered gain equals evaluateShowPropertyTrackV2 at 7 probes, and the forbidden re-normalized restatement is measured (0.5375 versus 0.4625 at 1400) rather than tolerated.",
    "prepareShowStageV2 status and typed refusal code/path/message with an unchanged preimage record.",
    "Real admitted route: provider write count, history length, saved record equality, reopened composition equality and delivered .epe elapsed at named times.",
    "Group conflict refusal names code invalid-result, the phrase 'overlaps active owner' and both effective track IDs on a fixture whose Clips do not collide.",
    "prepared.provenance.derivedSceneIds distinguishes the retained single-section emission from the sectioned one.",
    "Registered authenticated route specs drive the real pilot route for Group creation and Group occurrences with Fast/Precise capture sequences, 1024/390 layout bounds and an empty console."
  ],
  "redEvidence": [
    "Before the repair, 5 of the 8 new owner cases refused unsupported-transition-property-track: the instance-control, Clip-view, cross-boundary, curveSegment and shared-instance partitions.",
    "showV2IntegratedInsertSequence.test.ts previously asserted that p = 0 refused unsupported-pilot-record; the flipped partition fails on the pre-repair owner.",
    "showV2IntegratedGroupHoldSequence.test.ts previously had to delete the definition's internal Transition to demonstrate the §4 conflict.",
    "showGroupReplacementV2.test.ts previously had to clear the Group definition's propertyTracks to keep its sharing-user audit preparable."
  ],
  "faults": [
    "Reinstate the blanket full-Show activation guard: 8 cases fail across showSectionActivationPreparationV2, showV2IntegratedInsertSequence, showV2IntegratedGroupHoldSequence and showGroupReplacementV2.",
    "Drop per-section activation clipping (emit the unrestricted track per section): the cross-boundary and retained-kernel cases fail.",
    "Invert the Transition-window boundary test: both refusal partitions fail.",
    "Bypass the sectioned emitter and keep the single-section transition emission: the instance-control, cross-boundary and shared-instance cases fail. The Clip-targeted case survives this fault, because the legacy per-interval placement filter already scopes a placement-targeted track; the instance-target partitions are what require sections."
  ],
  "residualGaps": [
    "R16: a derived section boundary at or inside a participant Transition window still refuses. Owner proof, not an adapter guard: the legacy composition lowerer requires both participant placements in one derived Scene, and moving the boundary to the window edge would change the authored activation.",
    "The legacy Scene-scoped keyframe has no retained-curve descriptor, so a mid-segment section boundary has no exact legacy oracle; it is judged against the v2 evaluator with the forbidden alternative measured.",
    "Layout occurrence starts are deliberately not section boundaries in the participant route, so routing keeps its existing global recipe; per-section routing targets remain unproved and unused.",
    "Final authoritative committed-tip suites and Fable High review are coordinator-owned."
  ],
  "qualification": {
    "mutations": "4/4 scoped semantic faults killed (see faults); the surviving Clip-target case under fault 4 is recorded rather than hidden",
    "parity": "47 inventoried records matched, report hash unchanged",
    "locators": "0 new stale e2e locators (0 known stale, 55 unverifiable)",
    "focused": "showSectionActivationPreparationV2 8/8; full src/engine plus src/store 7004 tests in 446 files passed",
    "static": "npm run lint and npx tsc -b --pretty false clean",
    "route": "e2e/show-v2-group-create.auth.spec.ts and e2e/show-v2-group-occurrences.auth.spec.ts passed in an isolated authenticated runtime, as did the trimmed e2e/show-v2-layers.auth.spec.ts",
    "uiProof": "npm run check:ui-proof -- 8666d8890b1768d0da55268c62c44b540d5395bc HEAD exits 0; this slice changed no component TSX, App.tsx or stylesheet, and the existing 1038-stage-isolation record covers the range's 6 changed UI paths"
  }
}
