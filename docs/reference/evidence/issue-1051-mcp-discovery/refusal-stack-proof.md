# Final refusal-stack discovery proof (#1051)

At `c3a2b0a709e0905270587382d989402710e30c17`, installed
`codex-cli 0.153.4` called the local MCP Worker through app-server. The client
listed 101 stock Patterns, selected the returned CometLoom identity with four
exported controls, listed Controller profiles, and freshly read the live Show.
One server-minted operation created a Layer and Clip and reached applied/saved.
The [redacted transcript](codex-refusal-stack-client.json) records the exact
results on the final #1049 refusal contract.

The authenticated fixture passed 1/1 in 12.7 seconds. The saved capture shows
the connected client, one operation, two accepted changes, no unread outcome,
and the CometLoom Clip. The reopened capture and production API both retained
the returned Pattern reference. The proof record is
`.wrsp/ui-proof/1051-refusal-stack-discovery.json`. The fixture removed its
owned synthetic Show after capture.

This packet supersedes the current-protocol claim in the earlier post-#1048
packet; both older packets remain historical evidence for their stated code
tips. No model inference, hosted deployment, physical Controller, Scene
#1041/#1039 proof, authoritative suites, review, or landing is claimed.
