# Ordinary Clip Replace Pattern

Within the current ordinary Clip inspector, **Replace Pattern** has one explicit searchable Pattern choice and an Apply button. No source is preselected. It uses captured personal/built-in options; displayed name never resolves identity. Shared-user text explains that only this Clip gets an independent instance while other users retain state and controls. No extra independent checkbox or runtime target guessing.

Submitting resolves captured bundled source/public sliders and allocates each retained runtime/track/key identity once when changed shared replacement needs it. The request carries only source reference plus complete explicit identities. Same-source/name compatible no-op allocates nothing. Dirty selection resets on authored record/Clip changes. One guarded prepared adoption/history/save; failed, stale or unsupported candidate cannot alter other owners.

Trusted metadata resolution is a small pure seam reusable by the following Group-definition candidate; it executes no Pattern callback. Existing incompatible Group-control animation and compiler refusals remain explicit. Scope excludes Group replacement here, Effect removal, source/compiler/runtime rules and new store queue.
