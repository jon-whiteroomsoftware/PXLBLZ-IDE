# Native built-in cost table — Pixelblaze hardware

> **Pending a live run.** This file is overwritten by `npm run profile` against a
> physical Pixelblaze (see [README](./README.md)). Until someone runs it on the
> bench, the table below is a placeholder — do not cite these numbers.

**Device:** _not yet run_
**Firmware:** _not yet run_

| built-in | group | net µs/call | relative to mul |
|---|---|---|---|
| _run `PIXELBLAZE_IP=… npm run profile` to populate_ | | | |
